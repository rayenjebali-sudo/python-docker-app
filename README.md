#Python Docker App

A simple Python application that fetches data from the GitHub API and runs inside a Docker container.

##Features
- Uses Python to call a public API
- Containerized with Docker
- Lightweight and easy to run

---

##ech Stack
- Python
- Docker
- Requests library

---

##ow to Run

### 1. Build the image
```bash
docker build -t python-app .
